Cashfree UPI (Google Pay / PhonePe) — enable on merchant account
================================================================

Your checkout shows only "Pay by UPI ID / QR" inside the Android app WebView because
Cashfree needs BOTH:

1. Merchant feature flag (Cashfree dashboard → Support Form)
   Ask Cashfree to enable: "UPI Intent in JS SDK / WebView feature flag"
   for merchant: PREMium Mind

2. Native Android bridge (done in premium-mind-app MainActivity)
   - JavascriptInterface name: Android
   - Methods: getAppList(), openApp()
   - UPI deep links: upi://, tez://, gpay://, phonepe://, paytmmp://

After code changes:
- Rebuild APK from Android Studio (cap sync android → Run)
- Push index.html to Netlify (website mobile uses modal; app uses full page)

Website on phone Chrome: GPay/PhonePe should appear in Cashfree modal automatically.
If still missing on website, enable the same feature flag on Cashfree.

Optional Hostinger (create_order.php):
- Pass real customer phone (not 9999999999) for better UPI prefill
- return_url already points to verify_payment.php (app redirects back to index)
